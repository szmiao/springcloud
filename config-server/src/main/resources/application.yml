server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/szmiao/springcloud-config.git
          username: szmiao
          password: msz!0822
          search-paths: springcloud-config

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/